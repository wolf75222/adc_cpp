cmake_minimum_required(VERSION 3.16)
project(p1 CXX)
find_package(Kokkos REQUIRED)
add_executable(p1 phase1_transport.cpp)
target_include_directories(p1 PRIVATE ${POPS_INCLUDE})
target_compile_definitions(p1 PRIVATE POPS_HAS_KOKKOS)
target_link_libraries(p1 PRIVATE Kokkos::kokkos)
