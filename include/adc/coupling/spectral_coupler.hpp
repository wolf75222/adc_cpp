#pragma once
// Forwarding header (ADC-326): `spectral_coupler.hpp` now lives in `coupling/deprecated/`.
// This stub keeps the historical include path `<adc/coupling/spectral_coupler.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/deprecated/spectral_coupler.hpp>
