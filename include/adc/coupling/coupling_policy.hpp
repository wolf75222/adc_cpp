#pragma once
// Forwarding header (ADC-326): `coupling_policy.hpp` now lives in `coupling/base/`.
// This stub keeps the historical include path `<adc/coupling/coupling_policy.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/base/coupling_policy.hpp>
