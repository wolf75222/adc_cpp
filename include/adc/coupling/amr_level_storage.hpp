#pragma once
// Forwarding header (ADC-326): `amr_level_storage.hpp` now lives in `coupling/amr/`.
// This stub keeps the historical include path `<adc/coupling/amr_level_storage.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/amr/amr_level_storage.hpp>
