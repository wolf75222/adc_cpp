#pragma once
// Forwarding header (ADC-326): `amr_regrid_coupler.hpp` now lives in `coupling/amr/`.
// This stub keeps the historical include path `<adc/coupling/amr_regrid_coupler.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/amr/amr_regrid_coupler.hpp>
