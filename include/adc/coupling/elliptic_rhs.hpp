#pragma once
// Forwarding header (ADC-326): `elliptic_rhs.hpp` now lives in `coupling/base/`.
// This stub keeps the historical include path `<adc/coupling/elliptic_rhs.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/base/elliptic_rhs.hpp>
