#pragma once
// Forwarding header (ADC-326): `aux_fill.hpp` now lives in `coupling/base/`.
// This stub keeps the historical include path `<adc/coupling/aux_fill.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/base/aux_fill.hpp>
