#pragma once
// Forwarding header (ADC-326): `coupled_source.hpp` now lives in `coupling/source/`.
// This stub keeps the historical include path `<adc/coupling/coupled_source.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/source/coupled_source.hpp>
