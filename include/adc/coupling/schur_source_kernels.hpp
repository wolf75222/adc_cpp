#pragma once
// Forwarding header (ADC-326): `schur_source_kernels.hpp` now lives in `coupling/schur/`.
// This stub keeps the historical include path `<adc/coupling/schur_source_kernels.hpp>` compiling
// during the migration to the family layout. Prefer the canonical path for new code.
#include <adc/coupling/schur/schur_source_kernels.hpp>
