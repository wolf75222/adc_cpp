build/
build*/
cmake-build-*/
*.o
*.a
*.so
*.dylib
.DS_Store
.cache/
compile_commands.json

# Caches .so du DSL quand POPS_CACHE_DIR pointe DANS le repo (les demos exportent
# POPS_CACHE_DIR=$PWD/.pops_cache*) ; le defaut ~/.cache/pops/dsl est deja hors source.
.pops_cache*/

# documentation generee (Doxygen + Sphinx)
docs/_build/
docs/sphinx/_generated/
docs/sphinx/__pycache__/
docs/sphinx/doxygen/

# Python bytecode
__pycache__/
*.pyc

# Artefacts compiles du tutoriel (les figures/provenance sont versionnees)
docs/sphinx/tutorials/_assets/*.so

# Environnement pixi (l'env vit DANS le projet ; seul pixi.lock se committe)
.pixi/

# Fichiers de contexte / garde-fous d'agent : LOCAL-only, jamais commites ni pousses.
CLAUDE.md
CLAUDE.local.md
