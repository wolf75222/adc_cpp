build/
build*/
cmake-build-*/
*.o
*.a
*.so
*.dylib
.DS_Store
.cache/
compile_commands.json

# Caches .so du DSL quand ADC_CACHE_DIR pointe DANS le repo (les demos exportent
# ADC_CACHE_DIR=$PWD/.adc_cache*) ; le defaut ~/.cache/adc/dsl est deja hors source.
.adc_cache*/

# documentation generee (Doxygen + Sphinx)
docs/_build/
docs/sphinx/_generated/
docs/sphinx/__pycache__/

# Python bytecode
__pycache__/
*.pyc

# Artefacts compiles du tutoriel (les figures/provenance sont versionnees)
docs/sphinx/tutorials/_assets/*.so

# Environnement pixi (l'env vit DANS le projet ; seul pixi.lock se committe)
.pixi/
